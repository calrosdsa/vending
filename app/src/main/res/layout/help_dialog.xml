<?xml version="1.0" encoding="utf-8"?>
<android.support.constraint.ConstraintLayout
            xmlns:app="http://schemas.android.com/apk/res-auto"
            xmlns:android="http://schemas.android.com/apk/res/android"
            android:layout_width="fill_parent"
            android:layout_height="wrap_content"
            android:padding="15dp"
            android:background="@drawable/help_dialog_back"
            >

            <TextView
                android:id="@+id/load_text"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Ayuda Dialog "
                app:layout_constraintTop_toTopOf="parent"
                app:layout_constraintStart_toStartOf="parent"
                android:padding="3dp"
                android:textSize="25sp" />

            <ImageButton
                android:id="@+id/close_help_dialog"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:src="@drawable/baseline_close_24"
                app:layout_constraintTop_toTopOf="parent"
                app:layout_constraintEnd_toEndOf="parent"
                android:contentDescription="close_dialog"
                android:background="?attr/selectableItemBackgroundBorderless"
                />

      <LinearLayout
          android:layout_width="wrap_content"
          android:layout_height="wrap_content"
          app:layout_constraintTop_toBottomOf="@id/load_text"
          app:layout_constraintStart_toStartOf="parent"
          android:paddingTop="10dp"
          >

          <TextView
              android:layout_width="wrap_content"
              android:layout_height="wrap_content"
              android:text="I've previously found somewhere else an advice to set working directory to $MODULE_DIR$ in Defaults > Android JUnit but it's not enough - you need to also set $MODULE_DIR$ in all existing Android JUnit configurations (expand Android JUnit
              part of the tree) - or delete all of them as mentioned above."
              android:padding="3dp"
              android:textSize="15sp" />

      </LinearLayout>

            <!-- <ImageView
                 android:id="@+id/iv_route"
                 android:layout_width="wrap_content"
                 android:layout_height="wrap_content"
                 android:layout_centerHorizontal="true"
                 android:layout_centerVertical="true"
                 android:background="@drawable/dialog_bocop_loading_rotate_anim_img" />
            -->
</android.support.constraint.ConstraintLayout>


